-- LEGACY-033, расширение (ADR-018). Первый шаг снятия колонки EditionRights.legalBasisRu.
--
-- Своего правового основания у языковой редакции нет: основание привязано к стране
-- (решение владельца 21.09.2026, подтверждено 23.09.2026). Поле помечено @ignore в
-- prisma/schema.prisma, клиент этого релиза колонку не называет. Сама колонка остаётся:
-- её снятие — разрушительная миграция, она выкатывается отдельным заходом по команде владельца.
--
-- Схема базы здесь не меняется; комментарий колонки фиксирует её статус для того, кто смотрит
-- в базу, а не в код. COMMENT ON идемпотентен — повтор поверх применённого состояния безопасен.
COMMENT ON COLUMN "EditionRights"."legalBasisRu" IS 'LEGACY-033: not read or written since RIGHTS_CONTENT_HASH_V5; dropped by a separate owner-approved migration';
