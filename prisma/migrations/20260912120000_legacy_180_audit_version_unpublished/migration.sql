-- `LEGACY-180`, пачка `B28`: снятие версии с публикации обнуляет лицензионный снимок
-- и пишет об этом строку в журнал административных действий. Журналу нужны два новых
-- значения перечислений: действие `VERSION_UNPUBLISHED` и тип объекта `BOOK_VERSION`.
--
-- Миграция только добавляющая: ни `DROP`, ни `RENAME`, ни сужения типа. Значения
-- перечисления удалить нельзя вовсе, поэтому откат образа ничего не теряет - старый образ
-- просто не знает об этих значениях и их не пишет (ADR-018).
--
-- Идемпотентна намеренно, как и `20260911150000_legacy_015_admin_audit_event`: оборванный
-- накат оставил бы строку failed в `_prisma_migrations` и заблокировал все последующие
-- миграции до ручного `migrate resolve` на боевой машине, то есть до владельца.
--
-- `ADD VALUE` в Postgres 12+ вне транзакционного блока не требует перезаписи таблиц:
-- значение дописывается в каталог типа, данные не трогаются.

-- AlterEnum
ALTER TYPE "AdminAuditAction" ADD VALUE IF NOT EXISTS 'VERSION_UNPUBLISHED';

-- AlterEnum
ALTER TYPE "AdminAuditTargetType" ADD VALUE IF NOT EXISTS 'BOOK_VERSION';
