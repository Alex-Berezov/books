# Test/E2E environment example
# This file provides env variables used by e2e setup/teardown scripts.
# Copy to .env.test and adjust if needed. Jest can be configured to load it,
# but our e2e scripts load the base .env themselves and set PRISMA_TEST_* dynamically.

# Base connection used to derive ephemeral test DB URL.
# Must point to a Postgres instance with privileges to CREATE/DROP DATABASE.
# Example below matches local docker-compose defaults.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/books?schema=public"

# Redis for BullMQ (optional, leave empty to disable queues in tests)
# REDIS_URL=
# REDIS_HOST=
# REDIS_PORT=

# Internal variables used by e2e scripts (filled automatically at runtime):
# PRISMA_TEST_ADMIN_URL=postgresql://postgres:postgres@localhost:5432/postgres
# PRISMA_TEST_DB_NAME=e2e_1234567890

# Optional: tweak server binding during e2e (usually no need).
# PORT=5001
# HOST=127.0.0.1
