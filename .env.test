# Test/E2E environment
# This file is SAFE to commit - contains only test credentials, not production secrets.
# Used by e2e setup/teardown scripts and GitHub Actions CI.
# Must point to a Postgres instance with privileges to CREATE/DROP DATABASE.

# Base connection used to derive ephemeral test DB URL.
# For local development: postgres superuser (can create/drop databases)
# In CI: uses credentials from GitHub Actions services config
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/books?schema=public"

# Redis для BullMQ (опционально, можно оставить пустым для отключения очередей в тестах)
# REDIS_URL=
# REDIS_HOST=
# REDIS_PORT=

# JWT secrets for tests
JWT_ACCESS_SECRET="test_access_secret"
JWT_ACCESS_EXPIRES_IN=12h
JWT_REFRESH_SECRET="test_refresh_secret"
JWT_REFRESH_EXPIRES_IN=7d

# Disable rate limiting for tests
RATE_LIMIT_AUTH_ENABLED=0
RATE_LIMIT_GLOBAL_ENABLED=0
RATE_LIMIT_ENABLED=0

# Internal variables used by e2e scripts (filled automatically at runtime):
# PRISMA_TEST_ADMIN_URL=postgresql://postgres:postgres@localhost:5432/postgres
# PRISMA_TEST_DB_NAME=e2e_1234567890
